{
  "name": "creature-clash-prototype",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "vite build && mkdir -p dist/assets && cp -R assets/models assets/sprites dist/assets/",
    "preview": "vite preview --host 0.0.0.0",
    "dev": "vite --host 0.0.0.0"
  },
  "dependencies": {
    "pngjs": "^7.0.0",
    "three": "^0.164.1"
  },
  "devDependencies": {
    "vite": "^5.4.21"
  }
}
